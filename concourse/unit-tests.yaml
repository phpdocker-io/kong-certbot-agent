platform: linux

inputs:
  - name: kong-certbot-agent

image_resource:
  type: docker-image
  source:
    repository: phpdockerio/php72-cli

caches:
  - path: kong-certbot-agent/vendor

run:
  path: kong-certbot-agent/concourse/unit-tests.sh
