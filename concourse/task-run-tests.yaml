platform: linux

inputs:
  - name: code

outputs:
  - name: reports
    path: /tmp/reports

image_resource:
  type: docker-image
  source:
    repository: phpdockerio/php72-cli

caches:
  - path: code/vendor

run:
  path: code/concourse/tests.sh
